# Analysis -- 151. Reverse Words in a String

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Split and reverse using StringBuilder |
| **Time Complexity** | `O(n)` |
| **Space Complexity** | `O(n)` |
| **Language** | java |

---

## Optimal Solution

This solution achieves the theoretical optimum for this problem.

| Dimension | Complexity |
|---|---|
| Time | `O(n)` |
| Space | `O(n)` |

Splitting the string and reversing the words both require linear traversal of the input string, and storing the words in an array requires O(n) space.


---

## Code Readability

**Score:** [*****] (5/5)

The code uses clear variable names, concise logic, and standard patterns, making it easy to understand and follow.

---

## Interviewer's Perspective

**Verdict:**
The candidate implemented a clean and efficient solution using a standard approach for string manipulation. The code handles edge cases like extra spaces well and achieves optimal time and space complexity. This demonstrates strong problem-solving skills and attention to detail.

**Likely Follow-Up Question:**
> How would you modify this solution to handle very large strings efficiently if memory usage becomes a concern?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
