# Analysis -- 115. Distinct Subsequences

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Dynamic Programming (2‑D DP) |
| **Time Complexity** | `O(m·n)` |
| **Space Complexity** | `O(m·n)` |
| **Language** | java |

---

## Optimization Available

Replace the 2‑D table with a 1‑D DP array of length t.length+1, updating it from right to left to achieve O(n) space while keeping O(m·n) time.

### Target Options

**Time-Optimal: Dynamic Programming with 1‑D DP (rolling array)**

| Dimension | Complexity |
|---|---|
| Time | `O(m·n)` |
| Space | `O(n)` |

Both strings ≤1000, O(m·n)≈10⁶ fits, and a 1‑D array of size |t| gives minimal O(n) space.



---

## Code Readability

**Score:** [****-] (4/5)

Names i, j, m, n are standard and loops are clear, but the unnecessary 2‑D array and lack of braces make the code less concise.

---

## Interviewer's Perspective

**Verdict:**
The candidate chose a classic DP solution that correctly computes the answer, but it uses a full 2‑D table resulting in higher memory usage than needed. The implementation is straightforward and handles edge cases like |s|<|t|, yet it does not exploit the optimal O(n) space optimization. Overall the approach is correct but not space‑optimal.

**Likely Follow-Up Question:**
> How would you modify the DP to use only O(|t|) extra space instead of O(|s|·|t|)?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
