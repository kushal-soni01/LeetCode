# Analysis -- 153. Find Minimum in Rotated Sorted Array

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Binary Search (modified) |
| **Time Complexity** | `O(log n)` |
| **Space Complexity** | `O(1)` |
| **Language** | java |

---

## Optimal Solution

This solution achieves the theoretical optimum for this problem.

| Dimension | Complexity |
|---|---|
| Time | `O(log n)` |
| Space | `O(1)` |

Rotated sorted array can be searched by halving the interval each step, achieving O(log n) time with constant extra space.


---

## Code Readability

**Score:** [****-] (4/5)

Uses standard variable names and clear structure; lack of comments is minor.

---

## Interviewer's Perspective

**Verdict:**
The candidate implemented a binary‑search based solution that runs in O(log n) time and O(1) extra space, matching the optimal complexity for this problem. The code is concise and correctly handles edge cases such as a single‑element array, though the choice of comparing nums[mid] with nums[left] may be less conventional. Overall the solution is correct and efficient.

**Likely Follow-Up Question:**
> Can you explain why you compare nums[mid] with nums[left] to decide which half to discard, and whether this works for all possible rotations?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
