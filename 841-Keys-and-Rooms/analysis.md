# Analysis -- 841. Keys and Rooms

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Iterative Depth-First Search using a stack |
| **Time Complexity** | `O(n + m)` |
| **Space Complexity** | `O(n)` |
| **Language** | java |

---

## Optimal Solution

This solution achieves the theoretical optimum for this problem.

| Dimension | Complexity |
|---|---|
| Time | `O(n + m)` |
| Space | `O(n)` |

Linear traversal visits each room and each key once, matching the O(n) limits from n ≤ 1000 and total keys ≤ 3000.


---

## Code Readability

**Score:** [****-] (4/5)

Variable names are clear and the DFS logic is straightforward, though the extra HashMap is unnecessary.

---

## Interviewer's Perspective

**Verdict:**
The candidate implemented an iterative depth‑first search correctly and handles all edge cases, achieving optimal linear time and space. The code is readable but includes a redundant HashMap that could be removed.

**Likely Follow-Up Question:**
> Why did you choose to copy the rooms into a HashMap instead of accessing the original List directly?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
