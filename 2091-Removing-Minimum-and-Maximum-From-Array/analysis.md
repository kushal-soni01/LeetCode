# Analysis -- 2091. Removing Minimum and Maximum From Array

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Linear scan to locate min and max indices and compute minimal deletions |
| **Time Complexity** | `O(n)` |
| **Space Complexity** | `O(1)` |
| **Language** | java |

---

## Optimal Solution

This solution achieves the theoretical optimum for this problem.

| Dimension | Complexity |
|---|---|
| Time | `O(n)` |
| Space | `O(1)` |

Scanning once suffices to find min and max in O(n) time and O(1) extra memory, fitting n ≤ 10^5.


---

## Code Readability

**Score:** [****-] (4/5)

Variable names are clear and the algorithm is concise, though the final calculation could be commented for clarity.

---

## Interviewer's Perspective

**Verdict:**
The candidate implemented the optimal linear‑time solution and correctly handled the edge case of a single‑element array. The code is compact and uses only constant extra space, demonstrating good algorithmic understanding. Minor readability improvements, such as commenting the three deletion scenarios, would make the solution clearer.

**Likely Follow-Up Question:**
> Can you explain why the function returns 1 when the array length is 1, i.e., when the minimum and maximum are the same element?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
