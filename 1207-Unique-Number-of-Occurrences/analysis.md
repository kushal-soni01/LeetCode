# Analysis -- 1207. Unique Number of Occurrences

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Hash map for frequency counting and hash set for uniqueness check |
| **Time Complexity** | `O(n)` |
| **Space Complexity** | `O(n)` |
| **Language** | java |

---

## Optimal Solution

This solution achieves the theoretical optimum for this problem.

| Dimension | Complexity |
|---|---|
| Time | `O(n)` |
| Space | `O(n)` |

The constraints (n ≤ 1000) allow O(n) time, and a hash map and hash set achieve O(n) time and O(n) space by iterating through the array and storing counts and unique frequencies.


---

## Code Readability

**Score:** [*****] (5/5)

The code is well-structured, uses clear variable names, and follows a logical flow that is easy to understand.

---

## Interviewer's Perspective

**Verdict:**
The candidate implemented an optimal solution using hash maps and hash sets, demonstrating a solid understanding of algorithmic efficiency and data structures. The code is clean, readable, and handles edge cases effectively. This is a strong submission for an easy-level problem.

**Likely Follow-Up Question:**
> Why did you choose a hash set for checking uniqueness instead of sorting the frequency values and comparing adjacent elements?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
