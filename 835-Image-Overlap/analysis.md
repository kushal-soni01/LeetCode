# Analysis -- 835. Image Overlap

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Brute-force translation enumeration |
| **Time Complexity** | `O(n^4)` |
| **Space Complexity** | `O(1)` |
| **Language** | java |

---

## Space-Optimal Solution

This solution achieves the best possible space complexity for this problem.

| Dimension | Complexity |
|---|---|
| Time | `O(n^4)` |
| Space | `O(1)` |

### Time-Optimal Alternative

| Dimension | Complexity |
|---|---|
| Approach | 2D FFT convolution |
| Time | `O(n^2 log n)` |
| Space | `O(n^2)` |

Reading the n×n input requires O(n^2) time, and 2D FFT achieves the theoretical lower bound of O(n^2 log n) with O(n^2) extra space.


---

## Code Readability

**Score:** [****-] (4/5)

The loops and variable names are clear, making the O(n^4) logic easy to follow.

---

## Interviewer's Perspective

**Verdict:**
The candidate implemented a straightforward brute‑force solution that correctly enumerates all possible translations, but it runs in O(n^4) time which is far from the best known O(n^2 log n) approach. The code is readable and handles edge cases, yet it does not exploit more efficient convolution techniques. Overall, the solution is functionally correct but not optimal.

**Likely Follow-Up Question:**
> Can you describe how you might use a 2‑D FFT or a hash‑map of vector offsets to reduce the time complexity of this problem?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
