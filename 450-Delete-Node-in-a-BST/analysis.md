# Analysis -- 450. Delete Node in a BST

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Recursive BST delete using inorder successor/predecessor |
| **Time Complexity** | `O(n)` |
| **Space Complexity** | `O(n)` |
| **Language** | java |

---

## Suboptimal -- Both Dimensions Can Be Improved

Replace the recursive implementation with an iterative one that uses parent pointers; this reduces the auxiliary stack space from O(n) to O(1) while keeping the O(n) worst‑case time.

### Target Options

**Time-Optimal: Iterative BST delete (using parent pointers)**

| Dimension | Complexity |
|---|---|
| Time | `O(n)` |
| Space | `O(1)` |

Worst‑case height of an arbitrary BST is n, so deletion requires traversing at most n nodes, while an iterative implementation uses only constant extra variables.



---

## Code Readability

**Score:** [****-] (4/5)

Variable names are clear and follow conventions; the recursive structure is easy to follow, though lack of comments and extra helper methods slightly reduce clarity.

---

## Interviewer's Perspective

**Verdict:**
The candidate correctly implements the standard BST deletion algorithm and handles all cases, but the recursive approach incurs O(n) call‑stack space in the worst case. While the code is readable, it could be improved by eliminating recursion to achieve optimal space usage.

**Likely Follow-Up Question:**
> How would you modify your solution to avoid the O(n) recursion depth on a highly skewed tree?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
