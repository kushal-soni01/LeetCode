# Analysis -- 303. Range Sum Query - Immutable

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Prefix Sum |
| **Time Complexity** | `O(1)` |
| **Space Complexity** | `O(n)` |
| **Language** | java |

---

## Time-Optimal Solution

This solution achieves the best possible time complexity for this problem.

| Dimension | Complexity |
|---|---|
| Time | `O(1)` |
| Space | `O(n)` |

### Space-Optimal Alternative

| Dimension | Complexity |
|---|---|
| Approach | Naive Scan |
| Time | `O(n)` |
| Space | `O(1)` |

Scanning the original array for each query uses only O(1) extra memory but requires O(n) time per query.


---

## Code Readability

**Score:** [*****] (5/5)

Variable names and logic are clear and concise, making the code easy to follow.

---

## Interviewer's Perspective

**Verdict:**
The candidate chose the classic prefix‑sum technique, which yields O(1) query time and meets the optimal time‑space trade‑off for this immutable problem. The implementation is succinct, correctly handles edge cases, and uses standard naming conventions. Overall the solution is both correct and optimal for the given constraints.

**Likely Follow-Up Question:**
> How would you modify your solution to support updates to the array while still answering range‑sum queries efficiently?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
