# Analysis — 18. 4Sum

## � Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Two Pointers with Sorting and Nested Loops |
| **Time Complexity** | `O(n³)` |
| **Space Complexity** | `O(n)` |
| **Language** | java |

---

##  Optimality

This solution can be improved.

**Hint:** Consider using a Hash Map to store previously seen pairs of numbers, this could potentially eliminate the inner loop reducing the time complexity from O(n³) to O(n²).

| | Achieved | Target |
|---|---|---|
| **Approach** | Two Pointers with Sorting and Nested Loops | Hash Map and Sorting with Early Pruning |
| **Time** | `O(n³)` | `O(n² log n)` |
| **Space** | `O(n)` | `O(n)` |

---

## � Code Readability

**Score:** ⭐⭐⭐⭐☆ (4/5)

The code is mostly readable with clear variable names like left, right, and sum; however, a clear explanation of the logic and comments within the code could improve the readability further.

---

## � Interviewer's Perspective

**Verdict:**
The candidate has shown a basic understanding of the problem with a straightforward sorting and two-pointer approach. However, their code lacks optimization, and certain edge cases could be better handled. Additionally, comments explaining the logic would improve the code's clarity.

**Likely Follow-Up Question:**
> How would you modify your existing solution to handle extremely large inputs where memory is a concern, potentially optimizing it for both space and time?

---
<p align="center">
  Generated by **LeetLens**.<br>
</p>
