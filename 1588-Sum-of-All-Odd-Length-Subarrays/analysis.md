# Analysis -- 1588. Sum of All Odd Length Subarrays

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Prefix sum enumeration of odd-length subarrays |
| **Time Complexity** | `O(n^2)` |
| **Space Complexity** | `O(n)` |
| **Language** | java |

---

## Suboptimal -- Both Dimensions Can Be Improved

Use the element contribution method: each element arr[i] appears in ((i+1)*(n-i)+1)/2 odd-length subarrays, allowing O(n) time and O(1) extra space.

### Target Options

**Time-Optimal: Element contribution formula**

| Dimension | Complexity |
|---|---|
| Time | `O(n)` |
| Space | `O(1)` |

n ≤ 100 allows O(n) linear scan, and each element's contribution can be computed in constant extra space.



---

## Code Readability

**Score:** [****-] (4/5)

Variable names and structure are clear, though the inner loop stepping by 2 may require extra mental effort.

---

## Interviewer's Perspective

**Verdict:**
The candidate implemented a correct solution but used a brute‑force enumeration with O(n²) time and O(n) extra space, which is suboptimal for the problem. The code is readable and handles edge cases, but it does not exploit the known O(n) linear solution.

**Likely Follow-Up Question:**
> Can you explain how to compute the contribution of each element directly to achieve O(n) time without enumerating all subarrays?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
