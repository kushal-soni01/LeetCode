# Analysis -- 540. Single Element in a Sorted Array

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Binary search using index parity |
| **Time Complexity** | `O(log n)` |
| **Space Complexity** | `O(1)` |
| **Language** | java |

---

## Optimal Solution

This solution achieves the theoretical optimum for this problem.

| Dimension | Complexity |
|---|---|
| Time | `O(log n)` |
| Space | `O(1)` |

Sorted paired array allows halving the search each step, achieving O(log n) time with O(1) extra space for n ≤ 10^5.


---

## Code Readability

**Score:** [****-] (4/5)

Uses standard variable names and clear binary‑search logic, though brief comments would help.

---

## Interviewer's Perspective

**Verdict:**
The candidate correctly identified the binary‑search solution and implemented it with proper edge‑case handling; the code runs in O(log n) time and O(1) space as required. The implementation is concise and readable, using conventional naming without unnecessary complexity.

**Likely Follow-Up Question:**
> Can you explain why the parity check (mid%2) determines which side of the array contains the single element?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
