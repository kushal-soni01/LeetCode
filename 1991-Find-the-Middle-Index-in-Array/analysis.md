# Analysis -- 1991. Find the Middle Index in Array

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Prefix Sum with Iterative Validation |
| **Time Complexity** | `O(n)` |
| **Space Complexity** | `O(1)` |
| **Language** | java |

---

## Optimal Solution

This solution achieves the theoretical optimum for this problem.

| Dimension | Complexity |
|---|---|
| Time | `O(n)` |
| Space | `O(1)` |

The constraints (n ≤ 100) allow for linear iteration, and prefix sums can be computed in O(1) space by maintaining running totals.


---

## Code Readability

**Score:** [*****] (5/5)

The code uses standard variable names (totalSum, leftSum, nums) and is logically structured with clear iteration and condition checks, making it easy to follow.

---

## Interviewer's Perspective

**Verdict:**
The candidate implemented an optimal solution using prefix sums with iterative validation, achieving both time and space efficiency. The code is clean, handles edge cases like empty sides, and demonstrates a solid understanding of the problem constraints. Excellent submission overall.

**Likely Follow-Up Question:**
> How would you modify this solution if the array length constraint were increased to 10^5 or higher, and memory usage became a critical factor?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
