# Analysis -- 1979. Find Greatest Common Divisor of Array

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Iterative GCD with min-max extraction |
| **Time Complexity** | `O(n + log(min))` |
| **Space Complexity** | `O(1)` |
| **Language** | java |

---

## Optimal Solution

This solution achieves the theoretical optimum for this problem.

| Dimension | Complexity |
|---|---|
| Time | `O(n + log(min))` |
| Space | `O(1)` |

Finding min and max in O(n) and computing GCD in O(log(min)) is optimal given constraints (n ≤ 1000, nums[i] ≤ 1000).


---

## Code Readability

**Score:** [*****] (5/5)

The code is concise, uses standard variable names, and is easy to follow without unnecessary complexity.

---

## Interviewer's Perspective

**Verdict:**
The candidate implemented an optimal solution using a clear and efficient approach. The code is well-structured, handles edge cases like arrays of size 2, and adheres to the problem constraints. This demonstrates strong problem-solving skills and attention to detail.

**Likely Follow-Up Question:**
> Why did you choose to implement the GCD function recursively instead of iteratively, and what are the trade-offs of this decision?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
