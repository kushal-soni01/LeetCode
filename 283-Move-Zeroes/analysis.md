# Analysis -- 283. Move Zeroes

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Two Pointers with In-Place Swap |
| **Time Complexity** | `O(n)` |
| **Space Complexity** | `O(1)` |
| **Language** | java |

---

## Optimal Solution

This solution achieves the theoretical optimum for this problem.

| Dimension | Complexity |
|---|---|
| Time | `O(n)` |
| Space | `O(1)` |

The problem requires a single pass through the array to rearrange elements, which can be achieved in O(n) time with O(1) space by using two pointers to track positions of zeros and non-zeros.


---

## Code Readability

**Score:** [****-] (4/5)

The code is clear and uses standard variable names like 'left' and 'right', but the nested while loop could be slightly confusing without comments explaining its purpose.

---

## Interviewer's Perspective

**Verdict:**
The candidate implemented an optimal solution using the two-pointer technique, which is efficient in both time and space. The code is clear and handles edge cases like arrays with all zeros or no zeros. However, the nested while loop could benefit from a brief comment for clarity.

**Likely Follow-Up Question:**
> Why did you choose to use a nested while loop instead of incrementing the 'right' pointer in the main loop? Could this be simplified?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
