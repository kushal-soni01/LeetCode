# Analysis -- 60. Permutation Sequence

## Submission Summary

| Field | Value |
|---|---|
| **Approach Used** | Factorial number system with list removal (direct construction) |
| **Time Complexity** | `O(n^2)` |
| **Space Complexity** | `O(n)` |
| **Language** | java |

---

## Optimization Available

Use a Fenwick tree (Binary Indexed Tree) or segment tree to select and remove the idx‑th remaining number in O(log n) time, reducing the overall complexity to O(n log n).

### Target Options

**Time-Optimal: Factorial number system direct construction**

| Dimension | Complexity |
|---|---|
| Time | `O(n)` |
| Space | `O(n)` |

n ≤ 9 allows O(n) construction by selecting each digit using factorials, which is the fastest possible given the need to output n characters.



---

## Code Readability

**Score:** [****-] (4/5)

Variable names are short but clear, logic is straightforward, and StringBuilder avoids costly string concatenation.

---

## Interviewer's Perspective

**Verdict:**
The candidate implemented the classic factorial‑number‑system method but used an ArrayList removal that leads to O(n²) time. The code is clean and handles edge cases, yet it is not optimal for larger n. Overall the solution is correct but could be improved in efficiency.

**Likely Follow-Up Question:**
> Can you explain how you would modify the algorithm to achieve O(n log n) or O(n) time, for example by changing the data structure used for the remaining numbers?

---
<p align="center">
  Generated by LeetLens.<br>
</p>
